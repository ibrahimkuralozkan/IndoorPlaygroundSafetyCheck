<UserControl x:Class="IndoorPlaygroundSafetyCheck.Views.DeleteStationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:IndoorPlaygroundSafetyCheck.ViewModels"
             Cursor="Hand"  Width="800" Height="600">
    <UserControl.DataContext>
        <vm:DeleteStationViewModel/>
    </UserControl.DataContext>
    <Grid Margin="10" Background="#F2F2F2">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <ListView Grid.Row="0" ItemsSource="{Binding Stations}" SelectedItem="{Binding SelectedStation}" DisplayMemberPath="Name" Margin="0,0,0,10" Cursor="Hand" Background="#FFFFFF" BorderThickness="1" BorderBrush="#CCCCCC"/>
        <Button Grid.Row="1" Content="Delete Station" Command="{Binding DeleteStationCommand}" Click="DeleteButton_Click" Width="120" Height="40" Margin="5" Background="#F44336" Foreground="White" FontWeight="Bold" Cursor="Hand"/>
    </Grid>
</UserControl>
